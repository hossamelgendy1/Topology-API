package TopologyAPI.main.Controllers;

import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserController {/*
    @PostMapping("/user/readJSON/{fileName}")
    boolean readJSON(@PathVariable String fileName){

    };
    @PostMapping("/user/writeJSON/{fileName}")
    boolean writeJSON(@PathVariable String topologyID){

    };
    
    /*
TopologyList queryTopologies();
Result deleteTopology(TopologyID);
DeviceList queryDevices(TopologyID);
DeviceList queryDevicesWithNetlistNode(TopologyID, NetlistNodeID);*/
}
